module.exports = require('./dist/shared/lib/runtime-config')

// ADDED TO POSSIBLY FIX ISSUE WITH fs MODULE NOT FOUND ?!?!

// module.exports = {
//   webpack: (config, { isServer }) => {
//     // Fixes npm packages that depend on `fs` module
//     if (!isServer) {
//       config.node = {
//         fs: 'empty'
//       }
//     }
//
//     return config
//   }
// }
